/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'http2.mlstatic.com',
            'store.storeimages.cdn-apple.com'
        ],
    },
};

export default nextConfig;
